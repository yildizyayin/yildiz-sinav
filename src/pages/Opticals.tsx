import { useEffect,useState } from 'react';
import { CheckCircle2, CircleAlert, ScanLine } from 'lucide-react';
import { api } from '../api';
export function Opticals(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{void api<any>('/api/optical-templates').then(r=>setRows(r.templates))},[]);return <><div className="page-head"><div><span className="eyebrow">Optik kütüphanesi</span><h1>Optikler</h1><p>Tek şablon; dosya parser'ı, kamera geometrisi ve kişiselleştirilmiş baskı alanlarını birlikte taşır.</p></div></div><div className="exam-grid">{rows.map(t=><div className="exam-card" key={t.version_id||t.template_id}><div className="exam-top"><div className="quick-icon"><ScanLine/></div>{t.status==='READY'?<span className="verified"><CheckCircle2 size={15}/> Hazır</span>:<span className="warning"><CircleAlert size={15}/> Tanım gerekli</span>}</div><h3>{t.name}</h3><p>{t.vendor||'Genel'} · v{t.version||'—'}</p><div className="feature-flags"><span className={t.has_parser?'on':''}>TXT/DAT</span><span className={t.has_camera?'on':''}>Kamera</span><span className={t.has_print?'on':''}>Baskı</span></div></div>)}</div></>}
