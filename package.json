{
  "name": "yildiz-sinav-v1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "npm run build && vite preview",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run --config vitest.config.ts",
    "test:capacity:local": "node scripts/local-capacity-smoke.mjs",
    "test:capacity:live": "node scripts/live-capacity-smoke.mjs",
    "test:watch": "vitest --config vitest.config.ts",
    "deploy": "npm run build && wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply yildiz-sinav-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply yildiz-sinav-db --remote",
    "seed:generate": "node scripts/generate-demo-seed.mjs && node scripts/generate-standard-role-seed.mjs && node scripts/make-demo-seeds-idempotent.mjs",
    "seed:local": "npm run seed:generate && wrangler d1 execute yildiz-sinav-db --local --file=tmp/demo-seed.idempotent.sql && wrangler d1 execute yildiz-sinav-db --local --file=tmp/standard-role-seed.idempotent.sql && wrangler d1 execute yildiz-sinav-db --local --file=tmp/demo-camera-fixture.idempotent.sql && wrangler d1 execute yildiz-sinav-db --local --file=tmp/demo-standard-fixture.idempotent.sql",
    "seed:remote": "npm run seed:generate && wrangler d1 execute yildiz-sinav-db --remote --file=tmp/demo-seed.idempotent.sql && wrangler d1 execute yildiz-sinav-db --remote --file=tmp/standard-role-seed.idempotent.sql && wrangler d1 execute yildiz-sinav-db --remote --file=tmp/demo-camera-fixture.idempotent.sql && wrangler d1 execute yildiz-sinav-db --remote --file=tmp/demo-standard-fixture.idempotent.sql"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-router-dom": "latest"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "latest",
    "@cloudflare/workers-types": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "typescript": "latest",
    "vite": "latest",
    "vitest": "latest",
    "wrangler": "latest"
  }
}
